# 更新日志

## v2.4.0（2026-08-18）
### 新增：发光的沙漏配对
- **发光的沙漏**（`Charm_RightSpellCooldownHelper`）：游戏机制为沙漏启用时，右边一格的魔法书
  （`Charm_Magic`）CD 恢复速度 +30/60/100%（按沙漏等级）——整理会把沙漏放到 **CD 最长的魔法书左边**
- 实现：评分加"右边魔法书 CD 秒数 × `HourglassBonus`(6000/秒)"（沙漏须启用；CD 越长奖励越高 →
  搜索自动把沙漏配给 CD 最长的魔法书）；智能初始**沙漏先放**（自动避开最右列）并记录其右边格
  （+8000+CD×4000 加权引导魔法书，CD 越长越优先）；退火新增沙漏定向移动（把沙漏移到 CD 最长魔法书
  左边 / 把 CD 长的魔法书移到沙漏右边）
- 魔法书 CD 读取自 `Charm_Magic.ContainedMagic.cooldownTime`（ActiveSkillEntity）
- 可配置：`Synergy/HourglassItems`（key 列表，默认空，类识别已覆盖）、`Synergy/HourglassBonus`（每 CD 秒评分奖励）
- 日志新增：`沙漏@x,y：右边魔法书 CD=X.Xs`（或"未配对"）、识别统计 `沙漏X 魔法书Y`
### 待实测
- 背包放入发光的沙漏 + 多本 CD 长短不同的魔法书，按 F8 后确认沙漏在 CD 最长魔法书左边、日志配对正确

## v2.3.9（2026-08-18）
### 新增：奉献徽章同行聚拢
- **奉献徽章**（`Item_CompanionChaos_Name`，Charm_CompanionChaos）：游戏机制为徽章启用时，同一横排的
  同伴藏品（金色手铃/迷你弩炮/海塔·泰罗·艾哈迈德灵魂粉末/采矿作业总监臂章等，凡 `ICompanionCharm`）
  全部进入混沌强化态——整理尽量让徽章与同伴**同一横排**
- 实现：智能初始**徽章先放**并记录其所在行，同行空格加权（+6000）引导同伴同排；评分加"同行同伴数 ×
  DedicationCompanionBonus(3000/个)"（徽章须启用）；退火按评分自然同排
- 同伴识别用接口 `ICompanionCharm`（自动覆盖全部同伴藏品，无需逐个配置）；徽章可用 `DedicationBadgeItems` 扩展
- 可配置：`Synergy/DedicationBadgeItems`（key 列表）、`DedicationCompanionBonus`（每个同行同伴奖励）
- 日志新增：`奉献徽章@x,y：同行同伴 N 个`、识别统计 `奉献徽章X 同伴Y`
### 待实测
- 背包放入奉献徽章 + 同伴藏品，按 F8 后确认徽章与同伴同排、日志同行同伴数正确

## v2.3.8（2026-08-17）
### 新增：和谐之晶聚簇
- **和谐之晶**（`Item_NearLevelDamage_Name`，Charm_NearLevelDamage）：周围8格内神器每1级伤害放大+1%——
  整理尽量把高等级护符聚到它周围8格（须启用才生效）
- 实现：评分加"周围8格护符有效等级和 × HarmonyLevelBonus(2000/级)"奖励；智能初始和谐之晶先放
  并收集其8邻域格（+8000 加权）引导高等级护符聚集；退火按评分自然聚合
- 可配置：`Synergy/HarmonyCrystalItems`（key 列表）、`HarmonyLevelBonus`（每级奖励）
### 实测
```
3个和谐之晶互聚成簇 + 14级护符放三者公共邻域格：每个和谐之晶周围8格等级和=16
离线评分 4000→355004（+351004）；141ms
```

## v2.3.7（2026-08-17）
### 更新
- **红色行星观察日志（`Item_FlamePlanet_Name`）加入行星聚簇排除**：
  `Synergy/PlanetClusterExcludedItems` 默认现为 `Item_SuperPlanet_Name,Item_FlamePlanet_Name`
  （乐谱银河 + 红色行星观察日志）——两者虽是行星分类但不参与望远镜聚簇，按普通护符流程摆放。

## v2.3.6（2026-08-17）
### 修复
- **武器相关护符被永久判为"不启用"的 bug**：模型原用 `!isWeaponRelatedCharm` 直接禁用武器相关护符，
  导致丢弃的金戒指（大剑相关）等即使有加成也几乎拿不到。现改为**完整武器检查**（镜像游戏 flag3）：
  装备武器类型与 `relatedWeapon` 匹配才启用并正常计分，不匹配则不启用（与游戏一致）。
- 识别日志新增 `武器相关X(匹配Y)` 诊断。
- 说明：需在"装备大剑 + 背包有丢弃的金戒指"的局实测确认（本次测试局无武器相关护符）。

## v2.3.5（2026-08-17）
### 调整
- **背包初始化等待改回 3 秒**（`General/SessionStableDelay`）
### 新增：行锁定物品
- **凯尔萨德尼钥匙**（`Item_Lightning_Damage_Name`）：其藏品类型随所在**行**变化——整理时
  **保持用户所在行不变**，只在行内调整位置吃到加成（不可变行）
- 实现：智能初始选格限行（`FindBestCharmCell` 只考虑锁定的行）、行锁定物品放回原行行内最佳列
  （`FirstFreeInRow` 兜底）、评分跨行重罚（-100000 兜底，退火不会接受跨行布局）
- 可配置：`General/RowLockedItems`（逗号分隔多个 key，默认凯尔萨德尼钥匙）

## v2.3.4（2026-08-17）
### 调整
- **背包初始化等待缩短**：`General/SessionStableDelay` 默认 3 秒 → **1 秒**（进图后更快可整理；
  仍保留防"未就绪整理丢物品"的保护）
- **F8 提示简化**：改为「整理完毕」（去掉副文案）

## v2.3.3（2026-08-17）
### 优化
- **一次 F8 直达最佳**：每次整理内部跑**多轮独立搜索**（`Enhanced/SearchRounds`，默认 4 轮，
  每轮不同随机种子取全局最优）——等效于自动重复按 F8 多次，不再需要玩家手动多按。
  34 格满包约 200~300ms（仍属"很快"范围）。用户实测一次即达最佳。
- 说明：此前单次搜索为随机退火，偶尔停在局部最优需手动多按几次；多轮合并后单次即可。

## v2.3.2（2026-08-17）
### 新增
- **行星聚簇排除**（`Synergy/PlanetClusterExcludedItems`，默认 `Item_SuperPlanet_Name` 乐谱银河/谱子「银河」）：
  虽是行星分类（PLANET）但不参与望远镜聚簇——聚簇计数、智能初始聚簇摆放、退火行星移动三处均排除；
  该藏品按普通护符流程摆放（仍享受等级/位置/优先级逻辑）。需要更多排除物品时在配置列表追加 key。

## v2.3.1（2026-08-17）
### 修复
- **修复智能初始布局行星聚簇数组越界**（`IndexOutOfRangeException`）：
  `BuildSmartStart` 行星聚簇选格时 `ny*width+nx` 只经 `InBounds`(width,height) 检查、
  未校验 `idx < storage`——当背包为"非 6 的整数倍格数"（如 34 格/宽6，末行不完整）时访问越界。
  已补 `idx < storage` 边界检查（全面排查确认其余邻居访问点均已安全）。
- **隐藏本机构建路径**：csproj 增加 `PathMap`（`$(MSBuildProjectDirectory)=SephiriaBackpackOrganizer`），
  异常堆栈不再泄露 `C:\Users\...` 等本机绝对路径（保留行号便于排查）。
- 顺带修复 csproj 中因历史编码问题损坏的中文注释。

## v2.3.0（2026-08-17）
### 新增：联机客户端智能整理
- **此前**：Enhanced（智能排序）仅主机/单机可用，联机客户端自动回退到游戏内置排列（功能缩水）
- **现在**：联机作为客户端也能用完整智能排序——
  1. 客户端本地离线计算最优布局（复用智能初始 + 多起点退火）
  2. 生成「交换 + 旋转」操作序列（逻辑推演，含位置与旋转追踪）
  3. 用游戏自带网络接口逐步执行：`Swap`（CmdSwap 交换物品，requiresAuthority 对自身背包合法）+
     `DoClickAction`（CmdDoClickAction 旋转石板）
- **优点**：无需服务器权限、不清空背包（比主机版"清空-重写"更安全，不触发补货类问题）；
  其他玩家无需安装本插件即可配合
- 说明：需在"加入他人房间作为客户端"的场景实测验证（单机/主机走原 Enhanced 路径）

## v2.2.3（2026-08-17）
### 新增
- **指北针按伤害神器优先级配对**：多个伤害类神器（如行星）时，指北针配对奖励按上方神器的
  优先级加权（`PriorityWeight`）——高优先级伤害神器优先获得指北针加成；智能初始罗盘选格同步加权。
  用户实测确认功能正常。
- F8 提示文案改为「整理完毕(不满意可以再次按下优化)」
### 实测
```
护符7（5行星+1罗盘+1其他）优先级[P1:1 P2:3 P3:3]；罗盘@上方=伤害类
游戏评分 17010→57050（+40040）；46ms
```

## v2.2.2（2026-08-17）
### 更新
- **分层豁免格策略**（`Priority/IgnoreCellPreferredItems`，默认冰锁）：
  - **冰锁（`Item_ColdLock_Name`）**：只要背包里有解锁石板（IgnoreCriteria），就**尽可能利用豁免格**
    （+5000 加权，可解除位置限制上高等级格），其次才考虑其自然限制位置
  - **其他有位置限制的物品**：**先考虑自然满足位置**（+500 偏好），不行再用豁免格
  - 三处同步：智能初始选格（FindBestCharmCell 分档）、评分（EvaluateLayout 位置偏好）、
    退火定向移动（TryCriteriaMove：冰锁只去豁免格，普通受限只去自然格，冰锁无豁免格时回退自然格）
- 说明：需在"带冰冷的锁 + 有解锁石板"的局实测确认分层效果
### 实测
```
常规局：P1物品拿3级格、游戏评分 30280→32030、20-28ms（该局无冰锁/解锁石板）
```

## v2.2.1（2026-08-17）
### 更新
- **红茶叶袋（`Item_MiniBossFight_Name`）加入强制最高优先级**（默认 P1，与冰冷的锁/丢弃的金戒指/绝对戒指并列）
- **豁免格优先（IgnoreCriteria 石板协同）**：受限护符（有位置条件，如冰冷的锁）在智能初始选格时
  给豁免格 +5000 加权优先利用；退火定向移动也将豁免格计为满足条件——有解除限制石板时，
  优先让受限藏品站到豁免格（解除位置限制，可上高等级格）
### 实测
```
护符28：优先级[P1:1 P2:1 P3:3 P4:23]（优秀4 = P1×1[红茶叶袋/戒指] + P3×3；稀有→P2；普通23→P4）
游戏评分 46290→68040（+21750）；17-31ms
```

## v2.2.0（2026-08-17）
### 新增
- **藏品优先级系统**（`[Priority]` 段，替换原稀有度加权）：
  - 默认：传说=1、羁绊(永恒)=1、稀有=2、高级=3、普通=4（数值可配置）
  - **强制最高优先级物品**（`FixedHighPriorityItems`，默认三个）：冰冷的锁(`Item_ColdLock_Name`)、
    丢弃的金戒指(`Item_SweepRange_Name`)、绝对戒指(`Item_SweepRange_Enhanced_Name`)——即使稀有度不高也按 1 级优先
  - 等级分按优先级加权（1级×1.5 / 2级×1.25 / 3级×1.1 / 4级×1.0，可配置）
  - 智能初始布局：受限护符（位置条件）同级别内按优先级排；其余护符按优先级→稀有度排
  - 位置需求自动支持：带位置条件的藏品（如冰冷的锁）由运行时 criteria 识别并优先满足
- 识别日志显示 `优先级[P1:x P2:x P3:x P4:x]` 分布
### 实测
```
背包：丢弃的金戒指(优秀→强制P1) + 稀有藏品
优先级[P1:1 P2:1]；P1物品拿3级格、P2拿0级格；游戏评分 3000→33030；42ms
```

## v2.1.3（2026-08-17）
### 修复
- **修复快照校验误伤药水带**：v2.1.2 的一致性校验把药水带（inventoryMatrix 特殊位置 y=100）也计入实例集合，
  与只读正常背包格的快照不一致，导致"有药水就永远提示未就绪"。现改为只统计正常背包格
  （y 在高度内且索引在 storage 内），排除药水带等特殊位置。

## v2.1.2（2026-08-17）
### 防御（防止"背包初始化未完成时整理导致物品丢失"）
- 现象：进图后立即按 F8，偶尔背包开局物品全失、只剩心之重担（游戏会话刚建立时背包未就绪，
  CaptureState 快照不完整，"清空-重写"按不完整快照执行所致）
- **会话稳定检查**（`General/SessionStableDelay`，默认 3 秒）：进图后初始化期间按 F8 提示稍后再试，
  不执行任何操作；退出会话自动重置计时
- **快照一致性校验**：CaptureState 后对比快照与背包实际物品实例集合，不一致则中止整理（绝不碰背包）
- 注：本次为 v2.1.1 基础上的防御补丁，功能集不变（无宝珠处理）

## v2.1.1（2026-08-17）
### 回退稳定版
- **移除如意宝珠（宝珠类）全部处理代码**：因调试发现"背包宝珠条目在整理后翻倍"的异常，
  用户决定放弃该功能，回退到 v2.1.0 的功能集（神秘×2地块、望远镜/罗盘/负担/附魔/稀有度/性能优化全部保留）。
- 移除全部调试审计日志（清空/写回分段审计等），恢复干净代码。
- 说明：宝珠翻倍为游戏侧机制（起始物品补货）在"清空-写回"布局应用方式下被触发，与宝珠专属代码无关；
  若后续需要在背包保留如意宝珠时使用本插件，需将布局应用改为"增量移动"方式（另作评估）。

## v2.1.0（2026-08-17）
### 新增
- **神秘标签联动（×2 地块）**：识别"神秘"(MYSTIC) 分类藏品——≥2 个时 1 个神秘地块等级×2，≥5 个时共 4 个地块×2
  （对应游戏 ComboEffect_Mystic 机制）。插件读取游戏 `mysticPositions` + 组合计数，评分模型对 ×2 地块
  的护符等级按倍率计算，智能初始布局优先把高价值护符放到 ×2 地块上。
- 标签匹配大小写不敏感（实测游戏内为 `MYSTIC`）；配置 `[Mystic]` 段（Enable / Category / Multiplier）
- 诊断日志：识别行显示"神秘N个/×2地块M格"，并打印每个护符的实际标签
### 实测（2 个神秘护符 + 石板 + 负担）
```
神秘2个/×2地块1格；护符标签 [Item_KeelFragment→MYSTIC] [Item_FirstAttackBonusDamage→MYSTIC]
离线 -37500 → 52501（+90001）；游戏评分 23042 → 51051（+27009）；耗时 24-42ms
```

## v2.0.1（2026-08-17）
### 修复
- **修复格子越界崩溃**：`CurrentInventoryStorage` 不是 6 的整数倍时（如 34 格/6 宽，末行不完整），
  邻居格索引可能越界导致"整理背包异常"。全部邻居访问改为安全索引（`At()` + storage 边界检查）。
  实测：34 格满包（15石板+18护符+1负担）整理 53~69ms 稳定无异常。
### 调整
- 游戏内提示简化为「整理成功（不满意可再进行整理）」，详细评分/布局信息保留在日志

## v2.0.0（2026-08-17）
### 性能（重大）
- **全离线评分模型**：评估不再跑游戏权限周期，石板效果模板预计算 + 基础等级反推 + 附魔逐物品计入；
  单次评估微秒级，后期满背包整理从"十几秒卡顿"降到 **30~50ms**
- 多起点搜索（智能初始/原始/随机×2 各跑一轮取全局最优），默认 3000 迭代 × 3 重启全离线完成
- 移除 v1.x 增强模式中的游戏内置排列打底（那是卡顿主因）

### 新机制
- **行星望远镜**（Charm_PlanetModule）：启用时周围八格 PLANET 分类藏品获得加成——
  搜索会把行星聚拢到望远镜旁（聚簇权重 40000，默认高于行星自身等级差，保证"尽可能靠拢"）
- **指北针**（Charm_UpCharmDamage）：置于伤害类藏品(IAttackableCharm，含行星)或另一块指北针**下方**时生效，
  可链式叠加；未配对时等级分打一折（`Synergy/CompassUnpairedFactor`，效果只在配对时生效，与游戏一致）
- **附魔等级**：物品自身 Enchant 等级随物品计入格位等级，石板排布自动考虑
- **石板位置条件**：旗帜(最左列)/遮阳(最上行)等经 conditionQuery 边界 token 表达，摆位按游戏解析器语义评估条件
- **心之重担等负面藏品**：按 LocalizedString key（默认 `Item_MindBurden_Name`）识别，
  自动塞进最低/负等级格子（`Burden/NegativeCellPenalty` 强制）
- **稀有度优先**：高稀有度藏品评分额外加成（Rare+1200 / Legend+2000 / Eternal+3000），
  摆位按稀有度排序，低级藏品必要时被牺牲进负格

### 修复
- 修复离线模型按原始格子索引读取候选布局的严重 bug（改为按格子遍历 + 实例ID映射）
- 修复乘法效果在基础等级反推时被算成 0
- 修复智能初始布局使用残留缓冲导致评分退化为负
- 修复负担惩罚在无负格时误伤

### 实测（11 件：5石板 5护符 1负担）
```
望远镜@2,0：相邻行星 3 颗（3/3 全聚拢）
罗盘@2,2：上方=伤害类（行星正下方）
负担@5,3：格等级=-1（负格）
游戏评分 59781 → 104090；耗时 48ms
```

## v1.1.0（2026-08-16）
- 智能初始布局：负向石板负格推出背包外/压非护符下、受限护符优先满足条件格、IgnoreCriteria 豁免格利用
- 引导评分 + 定向移动；安全兜底

## v1.0.0（2026-08-16）
- 热键 F8 触发（兼容新输入系统）；Vanilla 内置排列暴露；Enhanced 模拟退火
